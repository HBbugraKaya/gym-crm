spring:
  config:
    activate:
      on-profile: stg
  datasource:
    url: ${GYMCRM_STG_DB_URL}
    username: ${GYMCRM_STG_DB_USERNAME}
    password: ${GYMCRM_STG_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
