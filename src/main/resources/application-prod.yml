spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: ${GYMCRM_PROD_DB_URL}
    username: ${GYMCRM_PROD_DB_USERNAME}
    password: ${GYMCRM_PROD_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
